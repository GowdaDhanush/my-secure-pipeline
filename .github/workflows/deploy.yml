name: Production-Grade Multi-Environment Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/my-secure-app:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/my-secure-app:${{ github.sha }}

      # ==========================================
      # NEW STEP: AUTOMATED SECURITY SCANNING
      # ==========================================
      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'docker.io/${{ secrets.DOCKERHUB_USERNAME }}/my-secure-app:${{ github.sha }}'
          format: 'table'
          exit-code: '0'  # This tells the robot: "If you find Critical bugs, BREAK the pipeline!"
          severity: 'CRITICAL,HIGH'

  deploy-to-staging:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Simulation
        run: echo "Successfully deployed version ${{ github.sha }} to Staging!"
        # ==========================================
  # NEW JOB: DEPLOY TO PRODUCTION (MANUAL GATE)
  # ==========================================
  deploy-to-production:
    needs: deploy-to-staging # Only allow production if staging went perfectly
    runs-on: ubuntu-latest
    environment: production  # This line activates the human gate rule you saved earlier!
    steps:
      - name: Production Simulation
        run: |
          echo "Connecting to LIVE production servers..."
          echo "Production deployment successful!"
